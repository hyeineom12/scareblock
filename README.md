# scareblock

2026-2 데이터사이언스프로젝트 (캡스톤 디자인)

## 개요

**지연 기반 다중 민감 콘텐츠 사전 차단 프레임워크.**

유튜브 영상을 3초 늦게 보여주고, 그 3초 동안 트리거 장면을 찾아 사용자에게 도달하기 전에 가린다.
점프 스케어(jump scare)는 이 구조가 필요한 유일한 카테고리이자 첫 flagship 구현체이고,
프레임워크는 처음부터 여러 카테고리를 겨냥한다.

> **용어** — 논문·발표에서는 구어인 "갑툭튀" 대신 국내 학술 논문에서 통용되는 **점프 스케어**를 쓴다.
> 사용자에게 일어나는 반응은 **놀람 반사**(startle reflex), 계산 문제로는
> **돌발 시청각 사건 탐지**로 부른다. 전체 용어표는 [docs/related-work.md](docs/related-work.md#용어)에 있다.

### 범위 선언

**대상은 유튜브 일반 업로드 VOD로 한정한다.** 영화·대여 등 EME(Widevine)가 걸린 콘텐츠는
픽셀·오디오 접근이 원천 차단되므로 평가셋과 데모에서 제외한다.
이는 구현 미비가 아니라 브라우저 DRM 규격상의 경계이며, 서버측 사전처리를 전제하는
별도 아키텍처에서만 다룰 수 있다.

## 핵심 아이디어

### 1. 지연 버퍼

갑툭튀는 소리와 화면이 동시에 터진다. "지금"을 보고 있으면 이미 늦는다.
화면을 3초 미뤄두면 AI가 먼저 읽을 시간이 생기고, 위험한 구간이 사용자에게 도착하기 전에 블러가 준비된다.

**서버측 사전처리 없이 클라이언트에서 실시간으로 하려면 지연이 반드시 필요하다**
— 이게 이 시스템이 존재하는 이유이자 논문의 주장이다.
사전 스캔으로 lookahead를 늘리는 경로는 아래 [보조 경로](#보조-경로--사전-스캔-선택)에서 별도로 다루며,
그 경로는 서버 비용과 지연을 대가로 지불하므로 클라이언트 실시간 경로를 대체하지 않는다.

**3초는 임의의 숫자가 아니다.** [Time Buffers](https://arxiv.org/abs/2010.03016)가 약 2~3초 버퍼에서
탐지 정확도 향상(THUMOS'14 mAP +13.8%)을 보고했다. 선행 연구에서 유효성이 확인된 구간을 채택한다.

### 2. lookahead 요구량 기준 분류

민감 콘텐츠를 "무엇인가"가 아니라 **"얼마나 미리 알아야 하는가"** 로 나눈다.
이 분류가 탐지 경로 배치를 결정하며, 프로젝트의 설계 주장이다.

| 클래스 | 카테고리 | 화면상 성질 | 지연 버퍼 | 탐지 경로 |
|---|---|---|---|---|
| **예측 불가형** | 갑툭튀 | 순간 사건 (소리·화면 동시 폭발) | **필수** | 오디오 규칙기반 → YAMNet 학습모델 |
| **지속 상태형** | 유혈 · 거미 · 주사기 · 사이렌 | 수 초간 유지 | 불필요 | CLIP zero-shot · 사전 스캔 |

지속 상태형은 프레임 N을 탐지해 N+1을 가려도 늦지 않는다. 지연 버퍼가 필요한 것은 예측 불가형뿐이며,
따라서 **갑툭튀는 카테고리 하나가 아니라 시스템의 제약 조건을 정의하는 클래스**다.

### 3. 저비용 카테고리 확장

카테고리별 파인튜닝 대신 CLIP zero-shot을 쓴다. 새 카테고리를 텍스트 프롬프트 교체만으로 추가할 수 있어
확장 비용이 거의 0에 가까워지고, 일정이 밀릴 때 "제일 먼저 자르는 항목"이 아니게 된다.
정확도가 부족한 카테고리 1개만 선별해 YOLO 파인튜닝으로 보강한다.

## 보조 경로 — 사전 스캔 (선택)

핵심 아이디어가 아니라 **선택적 확장 경로**다. [밀리면 자르는 순서](#밀리면-자르는-순서) 1순위이므로,
이것이 빠져도 위 세 가지는 그대로 성립해야 한다.

유튜브 VOD는 실시간 방송이 아니므로, 영상 로드 시점에 헤비 VLM(GPT-4o/Gemini)이 백그라운드로
앞부분을 미리 훑어 트리거 타임스탬프를 계산해둘 수 있다.
3초 지연 버퍼는 이 사전 스캔이 못 따라잡은 구간의 안전망으로 동작한다.

다만 이 경로는 서버 API 비용과 스캔 지연을 대가로 지불하며, 사용자가 영상을 열자마자
재생하는 일반적 시청 패턴에서는 스캔이 재생을 따라잡지 못한다.
**클라이언트 실시간 경로를 대체하는 것이 아니라 보강한다.**

## 관련 연구와 차별점

선행 조사 결과 **동일 조합을 다룬 연구는 없다.** 다만 개별 요소는 대부분 선례가 있으므로,
"우리가 처음 하는 일"이 아니라 **"따로 있던 것을 처음 잇는 구조"** 로 주장한다.

전체 조사 내역과 문헌 목록은 [docs/related-work.md](docs/related-work.md)에 있다.

### 지연 버퍼 기반 사전 차단 — PSE 연구에 선례 있음

가장 가까운 선행 계열이다. 광과민성 발작(PSE) 도메인에는 프레임 버퍼로 위험 자극을
사용자 도달 전에 완화하는 연구가 이미 있다.

- [Flikcer: A Chrome Extension to Resolve Online Epileptogenic Visual Content with Real-Time Luminance Frequency Analysis](https://arxiv.org/pdf/2108.09491) — 크롬 확장 + 실시간 영상 분석 + 유해 콘텐츠 차단. **구조적으로 가장 가까움**
- [Parallel scheme for real-time detection of photosensitive seizures](https://www.sciencedirect.com/science/article/abs/pii/S0010482516000123) — 실시간 스트리밍 탐지, 8프레임 버퍼
- [A new adaptive temporal filter: Application to photosensitive seizure patients](https://pubmed.ncbi.nlm.nih.gov/11145469/) — 프레임 메모리 버퍼로 사전 감쇠
- [Evaluating Conformance of Video Safety Tools for Photosensitive Epilepsy](https://pmc.ncbi.nlm.nih.gov/articles/PMC12249941/) — 해당 도구군의 평가 프레임워크

| | PSE 연구 | 본 연구 |
|---|---|---|
| **버퍼의 역할** | **시간축 필터링** (플리커 평활화) | **사건 탐지 후 도달 전 차단** |
| 탐지 대상 | 저수준 신호 (휘도 주파수) | **의미론적 사건** (갑툭튀·유혈·거미) |
| 카테고리 | 단일 고정 | 다중 · zero-shot 확장 |
| 버퍼 크기 | ~8프레임 (약 0.3초) | 3초 |

버퍼 크기는 역할이 다른 결과일 뿐 차별점이 아니다 — 크기를 늘려서 되는 일이 아니라,
**저수준 신호 평활화가 아니라 의미론적 사건 탐지를 버퍼 안에서 수행한다는 점**이 다르다.

→ **버퍼를 쓴다는 사실은 새롭지 않다.** 다만 "의미론적 사건을 잡는다"도 새롭지 않다 — 아래를 본다.

### 버퍼 시간을 탐지에 활용 — online action detection에 선례 있음

**주장이 가장 크게 겹치는 계열이다.**

- [Online Action Detection in Streaming Videos with Time Buffers](https://arxiv.org/abs/2010.03016) (Zhang et al., 2020)
  — 라이브 스트리밍의 송출 지연으로 생기는 **버퍼 시간을 탐지에 활용하는 문제 설정**을 제안한다.
  실험 버퍼는 **약 2~3초**, THUMOS'14 기준 mAP +13.8%
- [Real-time Online Video Detection with Temporal Smoothing Transformers (TeSTra)](https://arxiv.org/abs/2209.09236)
- [Online human action detection and anticipation in videos: A survey](https://www.sciencedirect.com/science/article/abs/pii/S0925231222003617)

이 계열은 **버퍼 안에서 의미론적 사건을 탐지한다.** PSE 비교표에서 "저수준 신호 vs 의미론적 사건"으로
갈라놓은 그쪽에 이미 서 있으므로, lookahead로 의미를 읽는다는 것만으로는 차별점이 되지 않는다.

| | Time Buffers (2020) | 본 연구 |
|---|---|---|
| **버퍼의 출처** | 라이브 방송에 **이미 존재하는** 송출 지연 | VOD에 **의도적으로 도입하는** 지연 |
| 카테고리별 lookahead 요구량 | **구분하지 않음** (균일 버퍼) | 예측 불가형 / 지속 상태형으로 분리 |
| 목적 | 탐지 정확도(mAP) | **사용자 도달 전 차단** |
| 실행 위치 | 언급 없음 (알고리즘 성능만) | **브라우저 클라이언트 실시간** |

→ **주장은 "버퍼 활용"이 아니라 "개입을 위해 없던 지연을 만들어낸 설계 결정"에 둔다.**
공짜로 있는 버퍼를 이용한 것과, 사용자에게 3초를 손해 보게 하면서까지 lookahead를 확보한 것은
설계 문제로서 다르다.

이 논문은 응용 예시로 "라이브 웹캐스트에서 NSFW가 탐지되면 실시간 red flag를 보낸다"를 들지만
**동기로만 언급하고 구현하지 않으며, red flag는 알림이지 차단이 아니다.**
본 연구는 그 자리를 구현하고 사용자 실험으로 검증한다.

### 점프 스케어 오디오 특징 — affective computing에 선례 있음

02단계에서 쓸 특징 설계는 이미 표준이다.

- [Affective Content Analysis in Comedy and Horror Videos by Audio Emotional Event Detection](https://ieeexplore.ieee.org/document/1521500/) — 공포 영상 오디오 감정 이벤트 탐지
- [Affective Video Retrieval: Violence Detection in Hollywood Movies](https://journals.plos.org/plosone/article?id=10.1371%2Fjournal.pone.0078506) — **energy entropy 기반 급작스러운 큰 소리 측정**, **혈흔 탐지**가 표준 특징
- [Affective Video Content Analysis: A Multidisciplinary Insight](https://dl.acm.org/doi/10.1109/TAFFC.2017.2661284) — 서베이
- [Multi-modal learning for affective content analysis in movies](https://link.springer.com/article/10.1007/s11042-018-5662-9)

→ 검증된 특징을 재사용하는 것이므로 안전하나, **"우리가 처음 제안한다"고 쓰지 않는다.**
결정적 차이는 이 계열이 전부 **오프라인 분석·검색**이 목적이고 **재생 중 개입이 아니라는 점**이다.

### 트리거 워닝 브라우저 확장 — 상용 제품 있음

- [PhobiaBlocker](https://chromewebstore.google.com/detail/phobiablocker/gjgghmahciffkcelboddbmkjkjchjnfk) — 공포증 이미지·영상 블러. **주변 텍스트 NLP** 기반
- [Trigger Warning Extension](https://chromewebstore.google.com/detail/trigger-warning-extension/emlcofoghclkckaajlejfoegblhahoff) · [Youtube Trigger Warning](https://chromewebstore.google.com/detail/youtube-trigger-warning/dhaighcmggigocehbgigdbmgpkhcekgl?hl=en) · SKIP IT — **크라우드소싱 타임스탬프·메타데이터** 기반

→ **픽셀을 보지 않는다.** 라벨이 없는 영상에는 무력하고 실시간이 아니다.
심사에서 "이미 있는 것 아닌가"가 나올 지점이므로 관련연구에 선제적으로 명시한다.

### 정리 — 무엇이 새로운가

| 주장 | 상태 |
|---|---|
| 지연 버퍼로 사전 차단 | ❌ PSE 연구에 선례 |
| 버퍼 시간을 탐지에 활용 | ❌ online action detection에 선례 |
| 버퍼 안에서 의미론적 사건 탐지 | ❌ 위와 동일 |
| 갑툭튀 오디오 특징 | ❌ affective computing에 선례 |
| 공포증 콘텐츠 블러 확장 | ❌ 상용 제품 존재 |
| **개입을 위해 없던 지연을 도입** | ✅ 기존 계열은 이미 있는 버퍼를 이용하거나 오프라인 분석 |
| **재생 중 개입 (online intervention)** | ✅ 기존 계열의 목적은 정확도·검색. Time Buffers도 NSFW 알림은 동기로만 언급하고 미구현 |
| **lookahead 요구량 기준 아키텍처 분류** | ✅ 선행 사례 확인되지 않음 |
| **의미론적 다중 카테고리 × 브라우저 내 실행 × zero-shot 확장의 조합** | ✅ 조합 자체가 공백 |

## 프로젝트 정보

| 항목 | 내용 |
|---|---|
| 기간 | 2026.09.08 – 12.10 |
| 인원 | 개발 2명 |
| 대상 | 유튜브 VOD (크롬 확장 MV3) |
| 주 트리거 | 갑툭튀 + 확장 카테고리 5종 이상 |
| 탐지 방식 | 규칙기반 → 학습모델(오디오+영상) → CLIP zero-shot |

## 팀

| 이름 | GitHub | 역할 |
|---|---|---|
| 엄혜인 | [@hyeineom12](https://github.com/hyeineom12) | 개발 A — 시스템 · 브라우저 |
| 이수현 | [@nocked115](https://github.com/nocked115) | 개발 B — 모델 · 데이터 · 평가 |

### 역할 정의

**개발 A — 시스템 · 브라우저**
확장 구조, 링버퍼, 오디오 지연, 블러 오버레이, 설정 UI, 추론 파이프라인 연결.

**개발 B — 모델 · 데이터 · 평가**
라벨링 기준과 평가셋, 특징 설계, 탐지 규칙과 학습 모델, 평가 스크립트와 실험 설계,
CLIP zero-shot 프롬프트 설계, VLM 비교 실험 총괄.

### 부하 분포

두 역할은 총량이 아니라 **시기**가 다르다. 일정 조정 시 이 비대칭을 전제한다.

- **개발 A** — 9~10월에 집중. M1(720p 무드롭 60초)이 크리티컬 패스라 초반이 빡세고, 11월은 개인화 UI 정도로 가벼워진다
- **개발 B** — 9월부터 11월까지 길게 깔린다. 라벨링이 꾸준히 시간을 먹고, 논문 숫자를 전부 만드는 자리라 05단계까지 안 끝난다

## 단계별 계획

### 00 · 전제 확정과 제안서 (09.08 – 09.10)

코드를 짜기 전에 "이게 되는가"부터 답한다.

- **개발 A** — 콘솔 검증 3종(DRM 여부 · 픽셀 접근 · 오디오 접근), 지연 재생 PoC
- **개발 B** — 관련 연구 조사(SponsorBlock, Where's the Jump?, LIRIS-ACCEDE,
  VLM 콘텐츠 모더레이션, 온라인 행동탐지 time buffer),
  **[docs/related-work.md](docs/related-work.md)의 읽는 순서에 따라 정독하고 차별점 문장 확정**,
  평가용 후보 영상 수집, 확장 카테고리 taxonomy 초안

**M0** — 3초 지연 재생 화면을 붙인 제안서 제출 + 확장 카테고리 taxonomy 초안

> 전제 검증은 아래 [기술 전제](#기술-전제-검증-결과)에서 답이 상당 부분 나와 있어 2~3일로 압축한다.
> 남는 기간은 01단계 지연 재생 완성도에 투입한다.

### 01 · 지연 버퍼 뼈대 (09.11 – 10.01)

탐지는 아직 없다. 여기서 둘의 작업이 갈라진다.

- **개발 A** — 크롬 확장 MV3 뼈대 + content script 후킹, 영상 링버퍼(VideoFrame 3초치),
  오디오 지연(WebAudio DelayNode), 블러 오버레이 + 음량 페이드다운
- **개발 B** — 평가셋 v1(영상 20개, 갑툭튀 타임스탬프 수동 라벨링 + 확장 카테고리 멀티라벨 동시 태깅),
  오디오 특징 추출 프로토타입(RMS, **상승 시간 dRMS/dt**, 스펙트럼 플럭스, 직전 정적 구간),
  **라벨 기준 문서화 — 아래 [클립 추출 규칙](#클립-추출-규칙)을 같은 문서에 포함한다**

**M1** — 가짜 타임스탬프로 블러 걸리는 계약 확정.
**정량 기준: 720p에서 드롭 프레임 없이 3초 지연 재생이 60초 이상 유지될 것.**

### 02 · 규칙 기반 탐지 — 돌아가는 첫 버전 (10.02 – 10.15)

여기까지 오면 논문이 성립한다. 뒤는 전부 성능 개선이다. **마지노선.**

- **개발 A** — 브라우저 실시간 오디오 특징 추출(AnalyserNode), 시크·일시정지·배속 대응,
  "안전 버퍼 채우는 중" 로딩 UI
- **개발 B** — 규칙 기반 탐지기(정적 → 음량 급등 + 저음 럼블), 평가 스크립트(precision/recall/F1 + 적시성)

**M2** — 실제 유튜브 영상에서 갑툭튀 자동 블러 — 데모 가능

### 03 · 학습 모델과 멀티모달 + VLM 비교 실험 (10.16 – 11.05)

규칙을 이기는 게 목표. 논문의 핵심 표가 여기서 나온다.

- **개발 A** — 프레임 캡처 → 시각 특징(컷 전환, 밝기 급변, 전역 모션),
  Web Worker 추론 파이프라인 + ONNX Runtime Web 연동,
  CLIP 모델 ONNX 변환 및 브라우저 내 프레임 임베딩 추론(1~2fps 샘플링),
  **WebGPU 미지원 환경(WASM 폴백) 동작 정의**
- **개발 B** — 평가셋 확장(영상 50~80개), YAMNet 임베딩 + 경량 분류기 학습 및 ONNX 변환,
  오디오·영상 융합 방식 비교(early/late fusion), CLIP zero-shot 프롬프트 설계 및 카테고리별 정확도 측정,
  (여유되면) GPT-4o/Gemini API로 동일 평가셋 오프라인 채점 → 상한선 벤치마크

**M3** — 학습 모델이 규칙 기반보다 낫다는 수치 확보(또는 아니라는 결론)
+ 규칙기반 / 학습모델 / zero-shot VLM 3자 latency-정확도 비교표

### 04 · 트리거 확장과 개인화 (11.06 – 11.19)

"프레임워크가 갑툭튀 전용이 아니다"를 보이는 단계. CLIP zero-shot 덕분에 저비용으로 지킬 수 있어졌으므로
**컷 목록에서 제외하고 핵심 기여로 다룬다.**

- **개발 A** — 민감도 슬라이더, 트리거별 켜기/끄기, 블러 모드 / 경고만 모드 선택, 설정 저장, 첫 실행 안내
- **개발 B** — 사이렌(YAMNet 기존 클래스에 임계값만), 거미·피·주사기 등 3종 이상을 CLIP zero-shot으로
  동시 커버(파인튜닝 없음), 정확도가 부족한 카테고리 1개만 YOLO 파인튜닝으로 보강

**M4** — 트리거 5종 이상 + 개인 설정 동작 — 확장성 입증

### 05 · 평가와 사용자 실험 (11.20 – 11.30)

논문에 들어갈 숫자를 전부 확정한다.

- **개발 A** — 실험용 빌드 고정, 데모 영상 촬영
- **개발 B** — 정량 평가 최종(P/R/F1, 적시성, 지연 시간별 성능 곡선), 실패 사례 분석,
  카테고리별 성능표(갑툭튀 vs 확장 카테고리, 규칙기반 vs 학습모델 vs zero-shot VLM)
- **공통** — 사용자 실험 15~20명(원본 vs 필터 시청, 놀람 정도 자가보고)

**M5** — 논문에 들어갈 표와 그래프 완성

### 06 · 논문과 발표 (12.01 – 12.10)

- 논문 작성 — 서론 / 관련 연구 / 시스템 / 실험 / 한계와 향후 과제
- 발표 자료 + 라이브 데모 리허설
- 저장소 정리, README, 재현 절차
  — 평가셋은 **URL + 타임스탬프 + 라벨만 공개**한다. 유튜브 영상을 내려받아 클립으로
  재배포하는 것은 ToS 문제가 있으므로 원본 미디어는 배포하지 않는다

**M6** — 제출

**논문 프레이밍** — "갑툭튀 차단 시스템"이 아니라
**"민감 콘텐츠를 lookahead 요구량으로 분류하고 각 클래스에 맞는 탐지 경로를 배치하는 지연 기반 프레임워크,
갑툭튀는 서버측 사전처리 없이는 지연이 반드시 필요한 클래스의 flagship 구현체로 검증"**.
CLIP zero-shot의 성능이 기대에 못 미쳐도 "적용 한계를 측정했다"로 성립하도록 결과 독립적으로 쓴다.

## 클립 추출 규칙

평가셋은 영상당 5분 이내 클립으로 자른다. 물량(5분 × 80개 = 400분)은 감당되지만,
**어디를 자르느냐가 숫자의 신뢰도를 결정한다.**

갑툭튀 구간을 알고 그 주변을 잘라내면 클립 내 갑툭튀 밀도가 실제 영상보다 훨씬 높아지고,
**precision이 낙관적으로 나온다.** 오탐률은 "갑툭튀가 없는 시간"이 충분해야 측정되는데
그 시간이 잘려나가기 때문이다.

- 클립 경계를 **사건 기준으로 잡지 않는다.** 무작위 오프셋으로 자른다
- **갑툭튀가 하나도 없는 대조 클립을 30% 이상 섞는다.** 이게 없으면 05단계 precision 숫자를 방어할 수 없다.
  **대조 클립은 전체 80개 안에서 채운다** — 80개에 얹는 것이 아니다. 즉 갑툭튀 포함 클립은 56개 이하가 되며,
  라벨링 총량은 늘지 않는다
- 위 규칙을 01단계 라벨 기준 문서에 함께 박아둔다

## 기술 전제 (검증 결과)

| 전제 | 상태 | 근거 |
|---|---|---|
| 픽셀 접근 | ✅ | 유튜브는 MSE(`blob:` URL)로 영상을 붙이므로 canvas가 taint되지 않는다 |
| 오디오 접근 | ✅ | 같은 이유로 `createMediaElementSource`가 무음을 뱉지 않는다 |
| DRM | ⚠️ | 일반 업로드 영상은 Widevine 없음. 영화·대여 콘텐츠는 EME로 차단된다 — 한계 절이 아니라 앞쪽 [범위 선언](#범위-선언)에서 scope로 선언한다 |

00단계 콘솔 검증 스니펫 — 유튜브 영상 재생 중 실행해 둘 다 `true`면 전제 확정:

```javascript
const v = document.querySelector('video');
const c = document.createElement('canvas');
c.width = 320; c.height = 180;
c.getContext('2d').drawImage(v, 0, 0, 320, 180);
console.log('pixel:', c.toDataURL().length > 1000);

const ac = new AudioContext();
const an = ac.createAnalyser();
ac.createMediaElementSource(v).connect(an);
an.connect(ac.destination);
setTimeout(() => {
  const d = new Uint8Array(an.frequencyBinCount);
  an.getByteFrequencyData(d);
  console.log('audio:', d.some(x => x > 0));
}, 1500);
```

## 인터페이스 계약

탐지기 → 재생기. 이 배열 하나가 전부다.

```javascript
[
  { time: 13.42, category: "jumpscare", confidence: 0.87, duration: 1.2, source: "rule" },
  { time: 48.10, category: "siren",     confidence: 0.64, duration: 3.5, source: "yamnet" },
  { time: 91.20, category: "blood",     confidence: 0.71, duration: 2.0, source: "clip-zeroshot" }
]
```

`source` — 어떤 모델이 탐지했는지 기록해두면 05단계 카테고리별·모델별 성능표 집계가 쉬워진다.

## 위험과 대응

| 위험 | 수준 | 대응 |
|---|---|---|
| **지연 재생 시청 품질** — canvas 재렌더로 전체화면·자막·재생컨트롤·화질설정이 무력화되고 1080p60 링버퍼는 수백 MB | 높음 | M1에 정량 기준(720p 무드롭 60초)을 걸어 조기 판정. 필요 시 해상도 상한을 명시적 제약으로 문서화 |
| **CLIP zero-shot 정밀도** — 붉은 조명을 피로 잡는 식의 오탐. [UnsafeBench](https://arxiv.org/pdf/2405.03486) 기준 경량 zero-shot VLM의 F1이 0.55 안팎, 최고치(GPT-4V)도 0.709 | 높음 | 04단계 목표를 **F1 0.6대**로 잡는다. 프롬프트 앙상블·임계값 튜닝. 논문을 결과 독립적으로 프레이밍해 수치가 나빠도 기여가 남게 함 |
| **라벨링 물량** — 영상 50~80개 멀티카테고리 타임스탬프를 1명이 담당. 순수 시청만 8~13시간, 구간 라벨링은 그 2~3배 | 높음 | **영상 길이 상한(5분 이내 클립)** 을 미리 고정. 초과 시 개수 하향 |
| **평가셋 편향** — 사건 주변만 잘라내면 갑툭튀 밀도가 과장돼 precision이 낙관적으로 나옴 | 높음 | [클립 추출 규칙](#클립-추출-규칙) — 무작위 오프셋 절단 + 대조 클립 30% 이상 |
| **CLIP 브라우저 추론 속도** — WebGPU에서는 1~2fps 여유, WASM 폴백은 프레임당 수백 ms | 중간 | 03단계에 WASM 폴백 동작 정의 포함. 미지원 시 사전 스캔 경로로 대체 |
| **CLIP은 단일 프레임 모델** — 시간축 사건인 갑툭튀에는 기여하지 않음 | 낮음 (설계상 의도) | 갑툭튀는 오디오 규칙 + YAMNet 담당. lookahead 분류가 이미 이 경계를 반영 |
| **공개 정답지가 전부 영화 기반** — Where's the Jump·LIRIS-ACCEDE 모두 범위 선언의 DRM 제외에 걸려 평가셋으로 사용 불가 | 낮음 | 라벨 기준 정의·특징 검증용으로만 사용. 평가셋은 직접 라벨링한다 — [docs/related-work.md §3.5](docs/related-work.md#35-결론--공개-데이터셋의-역할) |

## 밀리면 자르는 순서

| 순서 | 항목 | 방식 |
|---|---|---|
| 1st | 헤비 VLM 사전스캔 경로 | API 비교 실험만 남기고 서비스 통합은 제외 |
| 2nd | 개인화 UI | 민감도를 코드 상수로 고정 |
| 3rd | 시각 모달리티(영상 특징) | 오디오 단독으로도 갑툭튀는 잡힘 |
| 4th | 사용자 실험 | 정량 평가만으로 축소 |

트리거 카테고리 확장은 CLIP zero-shot으로 비용이 낮아져 **컷 목록에서 제외**한다.
마지노선은 02단계다.

## 폴더 구조

<!-- TODO: 폴더가 생기는 대로 채운다 -->

```
.
└── README.md
```

## 실행 방법

<!-- TODO: 환경 설정 · 의존성 설치 · 실행 명령 -->

## 협업 규칙

- `main` 직접 푸시 금지. feature 브랜치 → PR → 리뷰 1인 → merge.
- **코드 작업은 시작할 때 Draft PR을 먼저 열고 assignee를 단다.** 누가 무엇을 잡았는지 보여야 겹치지 않는다.
  커밋이 하나도 없으면 PR을 만들 수 없으므로 빈 커밋으로 연다 — `git commit --allow-empty -m "작업 시작"`.
  끝나면 Ready for review로 전환한다. 문서 수정 같은 단발 변경은 예외.
- **이슈는 따로 만들지 않는다.** 논의가 이슈와 PR 두 군데로 흩어지면 한쪽이 방치되므로 PR 한 곳에 모은다.
- 브랜치명: `<타입>/<요약>` 예) `feat/data-loader`, `fix/plot-axis`
- 커밋 메시지: 한 줄 명령형. 예) `데이터 로더 추가`
- 비밀값(API 키, 계정 정보)은 커밋하지 않는다.
