# 개인 작업 파일 — 저장소에 올리지 않는다
_local/

# 평가 데이터 — 영상·오디오는 커밋하지 않는다
# 재현 자료는 URL + 타임스탬프 + 라벨만 공개한다 (제안서 06단계)
*.mp4
*.webm
*.wav
*.m4a
# 명세 백업 — clipcut이 덮어쓰기 전에 남긴다. clips.csv 옆에 생겨 git add에 딸려 들어가기 쉽다
*.bak
*.bak[0-9]*

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# OS·에디터
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# 의존성·빌드 산출물
node_modules/
dist/
build/

# 도구 캐시
.ruff_cache/

# git worktree 잔여물 — 레포 전체가 복사돼 있어 add -A 에 딸려가면 두 배가 된다
wt*_path/
